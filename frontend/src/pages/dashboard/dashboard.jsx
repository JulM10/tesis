import Layout from "@/components/Layout";
import HorariosTable from "@/components/HorariosTable";

/**
 * Dashboard placeholder: se reemplaza por el dashboard real
 * (indicadores de ocupación, empleados activos, turnos del día) en FASE 4.
 */
export default function Dashboard() {
  return (
    <Layout>
      <HorariosTable />
    </Layout>
  );
}
