# URL base de la API del backend.
# En desarrollo local no hace falta crear .env (hay fallback a localhost:3000).
# En deploy (Vercel): setear la URL del backend en Railway.
VITE_API_URL=http://localhost:3000/api
