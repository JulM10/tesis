# Copiar este archivo a .env y completar los valores.
# .env NO se versiona (está en .gitignore).

# --- PostgreSQL ---
# Con Docker Compose el host es el nombre del servicio: postgres
# Corriendo el backend fuera de Docker (npm run dev local): localhost
DB_HOST=postgres
DB_PORT=5432
DB_NAME=hotel_yacanto
DB_USER=postgres
DB_PASSWORD=postgres

# --- Servidor ---
PORT=3000

# --- Auth ---
# Secreto para firmar JWT. Generar uno propio con:
# node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=reemplazar-por-un-secreto-largo-y-aleatorio
